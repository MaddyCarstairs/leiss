using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/*public delegate void MyStandartDelegate(); //Toda función que tenga void sin parámetros de entrada se considerará delegate con esto.

public class CombatController : MonoBehaviour {

    [SerializeField] UnityEngine.UI.Text combatText;

    event MyStandartDelegate startCombat; //En este evento se puede registrar quien tenga las características del delegate.
    
    
    // Use this for initialization

	void Start () {

        StartCoroutine(CombatSecuence());
	}
	
	IEnumerator CombatSecuence() {

        combatText.text = "Empieza en 3";
        yield return new WaitForSeconds(1);
        combatText.text = "Empieza en 2";
        yield return new WaitForSeconds(1);
        combatText.text = "Empieza en 1";
        yield return new WaitForSeconds(1);
        combatText.text = "YA";
        yield return new WaitForSeconds(1);
        combatText.text = "";
        startCombat();
    }

    public void RegisterOnCombatStart(MyStandartDelegate callback)
    {
        startCombat += callback;
    }
    
} */
